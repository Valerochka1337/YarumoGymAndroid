interface:
  display_name: "Android Product Discovery"
  short_description: "Focused product decisions and feature briefs"
  default_prompt: "Use $android-product-discovery to compare relevant options and recommend behavior within the requested scope."

policy:
  allow_implicit_invocation: true
